# Frontend production environment variables
# On Netlify: set these under Site Settings → Environment Variables

# Your AWS backend URL — NO trailing slash
# Must be HTTPS (required for SameSite=None cookies to work)
VITE_API_URL=https://your-backend-domain-or-ip.com
